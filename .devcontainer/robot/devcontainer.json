{
  "name": "robot-dev",
  "build": {
    "dockerfile": "../../robot/Dockerfile",
    "context": "../../robot"
  },
  "runArgs": [
    "--privileged",
    "--network=host",
    "--volume=/dev:/dev"
  ],
  "postCreateCommand": "cd robot && pip install -r requirements.txt"
}
